# المشروع القومي لقوائم الانتظار - نظام التعاقد على الخدمات الجراحية

## 📋 نظرة عامة

تطبيق ويب متقدم لإدارة طلبات التعاقد بين المستشفيات والمشروع القومي لقوائم الانتظار.

**الإصدار:** 1.0  
**آخر تحديث:** يناير 2026  
**الحالة:** Production Ready ✅

---

## 🚀 التشغيل السريع

### المتطلبات
- Python 3.8+
- SQLite3
- متصفح ويب حديث (Chrome, Firefox, Safari, Edge)

### التثبيت

```bash
# 1. استنساخ المستودع
git clone <repository-url>
cd waiting-list-app

# 2. إنشاء بيئة افتراضية
python3 -m venv venv
source venv/bin/activate  # على Windows: venv\Scripts\activate

# 3. تثبيت المكتبات
pip install -r requirements.txt

# 4. تشغيل التطبيق
streamlit run waiting_list_contracts_app.py
```

### الوصول إلى التطبيق
- **العنوان:** http://localhost:8501
- **اسم المستخدم الافتراضي:** admin
- **كلمة المرور الافتراضية:** admin123

---

## 📦 ماذا يوجد في المشروع

```
waiting-list-app/
├── waiting_list_contracts_app.py     # التطبيق الرئيسي (4800+ سطر)
├── backup_manager.py                 # نظام النسخ الاحتياطي التلقائي
├── requirements.txt                  # المكتبات المطلوبة
├── data/
│   ├── app.db                       # قاعدة البيانات (SQLite)
│   └── notifications.json           # بيانات الإشعارات
├── storage/                         # ملفات المستخدمين المرفوعة
├── exports/                         # ملفات التصدير
├── backups/                         # النسخ الاحتياطية
├── resources/                       # ملفات PDF للتنزيل
├── static/                          # الصور والملفات الثابتة
└── .gitignore                       # ملف استبعاد Git
```

---

## 👥 الأدوار والصلاحيات

### 1️⃣ المستشفيات (Hospital Users)
- **الوصول:** طلباتهم الخاصة فقط
- **الصلاحيات:**
  - تقديم طلبات تعاقد جديدة
  - تحديث بيانات المستشفى
  - رفع وإدارة المستندات
  - عرض حالة الطلبات
  - تغيير كلمة المرور

### 2️⃣ المراجع العام (Reviewer General)
- **الوصول:** جميع الطلبات
- **الصلاحيات:**
  - مراجعة جميع الطلبات
  - تحديث حالات الطلبات
  - تقييم المستندات
  - عرض الإحصائيات الشاملة
  - إدارة الإشعارات

### 3️⃣ مراجع القطاع (Reviewer Sector)
- **الوصول:** طلبات قطاعه فقط
- **الصلاحيات:**
  - مراجعة طلبات القطاع المخصص له
  - تحديث الحالات والتعليقات
  - عرض إحصائيات القطاع

### 4️⃣ المدير العام (Admin)
- **الوصول:** الكامل
- **الصلاحيات:**
  - إدارة المستخدمين (إنشاء، تعديل، حذف)
  - إدارة المستشفيات والخدمات
  - إدارة الإعدادات والحالات
  - نسخ احتياطي واسترجاع
  - عرض سجل النشاط الكامل

---

## 🔐 الأمان والبيانات

### حماية المستخدمين
- ✅ تشفير كلمات المرور (Salt + SHA256)
- ✅ نظام Rate Limiting لمنع Brute Force (5 محاولات في 5 دقائق)
- ✅ تفعيل WAL Mode لقاعدة البيانات (سرعة + أمان)
- ✅ عزل البيانات حسب الدور والقطاع
- ✅ حماية الجلسات (Session State)

### حماية البيانات
- ✅ نسخ احتياطي تلقائي يومي (قابل للتخصيص)
- ✅ حفظ آخر 30 نسخة احتياطية
- ✅ استرجاع آمن مع نسخ احتياطية وقائية
- ✅ سجل تدقيق شامل لجميع العمليات
- ✅ استخدام transactions في قاعدة البيانات

### خصوصية البيانات
- ✅ لا تسجيل كلمات المرور في الـ logs
- ✅ فصل البيانات حسب المستشفى والقطاع
- ✅ عزل دخول IP وتتبع الجلسات
- ✅ حذف الملفات المرفوعة عند حذف الطلب

---

## ⚙️ الإعدادات والتخصيص

### إدارة الخدمات والقطاعات

```python
# تفعيل/تعطيل الخدمات
# من لوحة التحكم: إدارة الإعدادات → الخدمات

# تخصيص المستندات الاختيارية
# من لوحة التحكم: إدارة الإعدادات → المستندات الاختيارية

# تعديل حالات الطلبات
# من لوحة التحكم: إدارة الإعدادات → حالات الطلبات
```

### النسخ الاحتياطي التلقائي

```python
# من لوحة التحكم: إدارة النسخ الاحتياطي

الإعدادات المتاحة:
- تفعيل/تعطيل النسخ التلقائي
- فترة النسخ (1-168 ساعة)
- الحد الأقصى للنسخ المحفوظة (5-100)
- عرض آخر نسخة احتياطية
```

---

## 🎯 أداء التطبيق

### تحسينات الأداء المطبقة

1. **قاعدة البيانات:**
   - ✅ WAL Mode لتقليل القفل
   - ✅ فهارس على الأعمدة الهامة
   - ✅ Cache Size = 10000
   - ✅ Temporary Store في الذاكرة

2. **Caching:**
   - ✅ `@st.cache_data(ttl=600)` للقوائم المشتركة
   - ✅ `@st.cache_data(ttl=1800)` للإعدادات
   - ✅ مسح تلقائي للـ cache عند التعديل

3. **رفع الملفات:**
   - ✅ دعم رفع متدفق (chunked) للملفات الكبيرة
   - ✅ حد أقصى 200MB لكل ملف
   - ✅ معالجة الأخطاء بدون فقد البيانات

4. **واجهة المستخدم:**
   - ✅ عدم إعادة تحميل غير ضرورية
   - ✅ استخدام `st.form` لمنع التحديث التلقائي
   - ✅ تنظيف الذاكرة المؤقتة دوري

---

## 🔧 استكشاف الأخطاء

### مشكلة: قاعدة البيانات مقفلة
```
الحل: انتظر 0.1 ثانية وأعد المحاولة (تم تطبيق آلياً)
```

### مشكلة: عدم ظهور الملفات المرفوعة
```
الحل:
1. تحقق من صلاحيات مجلد storage/
2. تأكد من وجود مساحة خالية كافية
3. أعد تحميل الصفحة (Ctrl+Shift+R)
```

### مشكلة: إشعارات مكررة
```
الحل: تم تطبيق نظام deduplication تلقائي
- استخدام event_key فريد لكل حدث
- حفظ في قاعدة البيانات ومتغيرات الجلسة
```

### مشكلة: أداء بطيئة
```
الحل:
1. قم بـ backup وحذف النسخ القديمة
2. تحقق من عدد السجلات في قاعدة البيانات
3. أعد تشغيل التطبيق لتنظيف الذاكرة
```

---

## 📊 الإحصائيات

### استعلامات محسّنة
- ✅ تحليل حسب الحالة / الخدمة / القطاع / المحافظة
- ✅ رسوم بيانية تفاعلية (Plotly)
- ✅ تصفية متقدمة بتاريخ النطاق
- ✅ تصدير إلى Excel

---

## 📨 نظام الإشعارات

### أنواع الإشعارات
1. **إنشاء طلب:** للإدمن والمراجعين
2. **تحديث الحالة:** للمستشفى والمراجعين
3. **إضافة مستند:** للإدمن والمراجعين
4. **إنشاء مستشفى:** للإدمن والمراجعين

### منع الإشعارات المكررة
- ✅ فحص `event_key` في قاعدة البيانات
- ✅ حفظ العلم في `session_state`
- ✅ حد أقصى 50 إشعار مرة واحدة

---

## 🌐 النشر على Streamlit Cloud

### الخطوات:
1. ادفع الكود إلى GitHub
2. اذهب إلى https://share.streamlit.io
3. انقر "New app" واختر المستودع
4. حدد `waiting_list_contracts_app.py` كملف الدخول
5. انقر "Deploy"

### الملفات المطلوبة:
- ✅ `requirements.txt` - المكتبات
- ✅ `waiting_list_contracts_app.py` - الملف الرئيسي
- ✅ `backup_manager.py` - نظام النسخ الاحتياطي
- ✅ `static/` - الصور (إن وجدت)
- ✅ `.gitignore` - استبعاد البيانات الحساسة

### ملفات لا يجب رفعها:
- ❌ `data/app.db` - قاعدة البيانات
- ❌ `storage/` - الملفات المرفوعة
- ❌ `backups/` - النسخ الاحتياطية
- ❌ `credentials*.xlsx` - الاعتمادات
- ❌ `.streamlit/secrets.toml` - الأسرار

---

## 📝 ملاحظات الصيانة

### الفحص الدوري
- [ ] التحقق من مساحة القرص المتبقية
- [ ] مراجعة سجلات الأخطاء
- [ ] تنظيف النسخ الاحتياطية القديمة
- [ ] تحديث المكتبات والمتطلبات

### النسخ الاحتياطي
- تشغيل يومي تلقائي (افتراضي)
- حفظ آخر 30 نسخة
- يمكن التشغيل اليدوي من لوحة التحكم

### سجلات النشاط
- حفظ جميع العمليات في `activity_log`
- حفظ تغييرات البيانات في `audit_log`
- متاح من: إدارة الإعدادات → سجل النشاط

---

## 🐛 الإصلاحات الحديثة (v1.0)

### تحسينات الثبات
- ✅ إصلاح نظام رفع الملفات (منع المعالجة المكررة)
- ✅ تحسين معالجة الاتصالات المقفلة
- ✅ نظام deduplication للإشعارات
- ✅ تنظيف الذاكرة التلقائي

### تحسينات الأمان
- ✅ تشفير محسّن لكلمات المرور مع salt
- ✅ Rate limiting لمنع Brute Force
- ✅ تدقيق شامل للعمليات
- ✅ عزل البيانات حسب الدور

### تحسينات الأداء
- ✅ استخدام WAL mode للقاعدة
- ✅ فهارس محسّنة
- ✅ caching ذكي
- ✅ رفع متدفق للملفات

---

## 📞 الدعم والمساعدة

### التواصل
- 📧 البريد الإلكتروني: [البريد]
- 📱 الهاتف: [الرقم]
- 🌐 الموقع: [الموقع]

### التوثيق الإضافية
- راجع `IMPLEMENTATION_SUMMARY.md` للتفاصيل التقنية
- راجع `MISSING_FEATURES_ANALYSIS.md` للميزات المستقبلية
- راجع `NOTIFICATIONS_IMPLEMENTATION_PLAN.md` لنظام الإشعارات

---

**آخر تحديث:** يناير 17, 2026  
**الحالة:** ✅ Production Ready  
**الإصدار:** 1.0.0
